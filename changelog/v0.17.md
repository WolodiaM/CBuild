---
title: v0.17
date: 2026-08-29
---

# PoolAlloc.h

- Added pool allocator (allocate from a pool of objects, pool can be
  resized dynamically and allocator uses linked list of chunks to
  keep pointers to objects stable). (@WolodiaM)
  * `cbuild_pool_alloc_object_init_t`.
  * `cbuild_pool_alloc_object_reset_t`.
  * `cbuild_pool_alloc_t`.
  * `cbuild_pool_alloc_new`.
  * `cbuild_pool_alloc_malloc`.
  * `cbuild_pool_alloc_free`.