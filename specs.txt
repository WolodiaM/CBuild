General:
+	Written in c99
+	Single-header library and possibly proper library
-	Support for all major OS (only libc as dependency and possibly pthread)
+	Possibility to bootstrap buildsystem with `gcc -o CBuild.run CBuild.c` when `CBuild.c` includes `CBuild.h`
Compilation:
+	Macro to select OS compiler and compiler args
+	Self-rebuild
-	Build metadata and caching
Container libs:
+	Dynamic array implementation
+	String-view lib
+	String lib (based on da)
Command execution engine:
-	System wrapper
+	Sync and async commands, pthread support
FS libs:
+	Filesystem wrapper
+	File reader and writer
General libs:
+	Logger
