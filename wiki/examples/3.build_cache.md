---
title: Example of buildscript with object file caching
---
# Recompile files only if needed

::: entry

Here project also consists of 2 source files (`utils.c` and `main.c`) and one header (`utils.h`). Buildscript rebuilds both source files in parallel and also caches objects that do not need to be changed.

The core of buildscript is almost the same as before - it contains pointer to [`cbuild_proclist_t`](/doc/Proc.html#ID_cbuild_proclist_t) array `procs`, scratchpad [`cbuild_cmd_run`](/doc/Command.html#ID_cbuild_cmd_run) `cmd`, uses in identical way [`cbuild_cmd_run`](/doc/Command.html#ID_cbuild_cmd_run) and then also calls [`cbuild_procs_wait`](/doc/Proc.html#ID_cbuild_procs_wait) for sync. But now each call to [`cbuild_cmd_run`](/doc/Command.html#ID_cbuild_cmd_run) is wrapped into `if` block that checks if [`cbuild_compare_mtime`](/doc/Compile.html#ID_cbuild_compare_mtime) returns value greater than 0. This just checks if modification time of input file (second argument) is newer than output file (first argument).

:::

::: entry

### Code

Directory structure:
```{.tree dir=examples/build/3.cache}
```

App's source code.

`main.c`
```{.c .lineNumbers file=examples/build/3.cache/src/main.c}
```

`utils.h`
```{.c .linenumbers file=examples/build/3.cache/src/utils.h}
```

`utils.c`
```{.c .linenumbers file=examples/build/3.cache/src/utils.c}
```

`cbuild.c` - Your buildscript:
```{.c .linenumbers file=examples/build/3.cache/cbuild.c}
```

`cbuild.h` - Build system core.

:::

::: entry

### How to run

```bash
cc -o cbuild.run cbuild.c
./cbuild.run
./app.run
```

:::