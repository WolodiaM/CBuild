This will not work. I need to parse file into "blocks" and then cross-link them.
----------------------------------------
window.addEventListener('DOMContentLoaded', () => {
    const hash = window.location.hash.substring(1);
        const entry = document.getElementById(hash);
        if (entry) {
            // Find the first link inside the element (or the element itself)
            const link = entry.tagName === 'A' ? entry : entry.querySelector('a');
            if (link && link.href) {
                window.location.replace(link.href);
            }
        }
    }
});
